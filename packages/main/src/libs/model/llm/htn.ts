/**
## 基于不可变状态的 LLM-Prolog 混合任务规划架构说明

针对 Node.js 环境下原生 HDDL/HTN（分层任务网络）生态匮乏的现状，以及大模型（LLM）在模拟内存状态“破坏性修改”（如数据删除与重写）时极易产生幻觉的痛点，在此提出一种基于“不可变状态”的 LLM → Prolog 混合规划范式。该构想尝试通过引入 Prolog 逻辑编程，在不依赖传统 HTN 引擎的前提下，实现等效且更可靠的符号规划。

------------------------------
## 一、 核心设计思想：不可变状态与 HTN 的逻辑等效
本构想的核心点在于改变传统自动规划对“世界状态”的维护方式，将时序引入一阶谓词逻辑：

* 状态的不可变性（Append-only）：系统完全摒弃“删除旧状态、写入新状态”的传统模式。世界状态被视为一条不可变的、仅允许追加的“事实链”。
* 通过时间/情形统一规避状态破坏：动作（Action）的执行不会抹除过去。例如，“汽车从上海开往北京”这一事件，在系统中表现为：旧时间点 $t_0$ 存在事实 at(car, shanghai, t0)，动作执行后，直接追加新事实 at(car, beijing, t1)。
* HTN 到 Prolog 的映射：由于消除了状态改变的副作用，HTN 中的“复合任务”与“分解方法（Method）”，可以完全等效地映射为 Prolog 中的“总目标（Goal）”与“逻辑子句（Rule）”。Prolog 天然的自上而下分解、统一化（Unification）和回溯（Backtracking）机制，将被直接用作系统的 HTN 规划器。

------------------------------
## 二、 系统三层架构与工作流
在 Node.js 环境下，整个规划流程被划分为三个职责明确的层级：

[ 1. LLM 数据生成层 ] ──(结构化JSON)──► [ 2. Node.js 转换层 ] ──(Prolog文本)──► [ 3. Prolog 规划执行层 ]
  负责意图理解与规则直觉                   负责Schema校验与文本拼装                  负责严格的符号推导与路径生成


   1. 大模型（LLM）数据生成层（直觉与输入）
   * 利用大模型的泛化能力理解用户的复杂高层意图。
      * 通过 Structured Outputs (JSON Mode) 进行强约束，让 LLM 仅负责输出“当前环境的原始事实（Facts）”、“最终要达成的总目标（Goal）”以及特定场景下的“动态分解规则（Rules）”。
   2. Node.js 中间件转换层（桥梁与组装）
   * 发挥 Node.js 擅长处理 JSON 异步 IO 的优势，利用 ajv 等库对 LLM 回传的数据进行合规性校验。
      * 将结构化的 JSON 数据，通过轻量级模板一键转换为符合 Prolog 语法的标准逻辑文本（.pl 文件）。
   3. Prolog 核心规划执行层（理性与校验）
   * Node.js 通过进程控制（如调用全局 SWI-Prolog）或纯 JS 虚拟机（如 Tau-Prolog）触发 Prolog 解释器。
      * Prolog 结合 LLM 生成的原始数据，在不可变的事实库上进行确定性的定理证明。如果某条路径在特定时间点不满足前置条件，Prolog 会自动触发高效回溯，最终输出一条安全、严谨且无逻辑冲突的原子动作序列（Plan）。
   
------------------------------
## 三、 试图解决的我呢提

   1. 极大降低 LLM 的推理负担：不可变的状态流让大模型无需在长文本中痛苦地模拟“执行 5 步动作后内存还剩什么”，只需专注于单步的模式匹配，能显著降低幻觉率。
   2. 绕过 Node.js 的生态短板：不需要在 Node.js 中重新造一个不成熟的 HDDL 解析器或 HTN 引擎，而是通过逻辑转换，直接复用了 Prolog 历经几十年工业验证的超高符号搜索性能。

------------------------------
基于这一初步设想，后续准备着手进行技术验证。针对将 JSON 数据转换为 Prolog 文本的中间件设计，以及如何利用 JSON Schema 来精确规范大模型的输出边界，在工程落地时，可能还需要重点防范以下潜在的边缘 case（边界情况）：

* 无限循环规则防御：LLM 可能会无意间生成逻辑闭环的动态规则（例如 A :- B. 且 B :- A.），需在 Prolog 端或中间件加入递归深度限制。
* 时间步膨胀导致搜索爆炸：随着不可变事实的追加，Prolog 的统一化检索耗时可能会随着长周期任务线性增长，需设计定期“状态剪枝”或“快照重置”机制。

 */