/**
 * ============================================================================
 * 【P-12 · 提示词:实现路径判定(形态分流+链路规划版)】
 * ============================================================================
 * FORM    形态分类(极简,单次,哨兵输出)。判据:LLM 的原始输出能否直接就是该
 *         文件本身。文本芯二进制壳(doc/pdf/xlsx等)一律 binary。
 * COMPOSE 文本成分拆解(Prism 收敛)。
 * CHAIN   二进制链路规划(Prism 收敛):只产"程序调用序列",不再让 LLM 列外部输入
 *         ——外部输入由 resolveChain 从 steps 反推派生(guard)。最终输出名由外部
 *         给定,LLM 与审核员均不得改写。每步须给真实程序候选。
 *
 * 【本版核心改进:交付物/信息必须具体到字段级】
 *   name/intent 是整个规划体系里唯一的数据接口,泛化的命名会污染下游全部递归。
 *   统一由 NAMING_SPEC 约束:宁可自造词汇,也要把每个交付物/信息锚定到唯一、
 *   具体、脱离上下文可懂的语义。宏观但精确——限定"是什么",不展开"里面有什么"。
 */
import { z } from 'zod';
import { FORM_BINARY, FORM_OTHER, FORM_TEXT, NO_ISSUE_MARK } from '../config.js';

/* ==================== 统一命名规范(COMPOSE/CHAIN 共用) ==================== */

/**
 * 所有产出 名称｜说明 的地方都内联本规范。这是硬约束,不是建议。
 */
export const NAMING_SPEC = `【命名与说明规范(硬约束)】
你产出的每一个交付物/信息,都是一个"名称 + 说明"的二元组,它将成为下游递归与程序调用的唯一数据接口。命名泛化会污染整条链,务必做到:

1. 具体到不可再指代:名称必须唯一锚定"这一个"东西,而不是一类东西。
   ✗ 泛化:"文本"、"内容"、"数据"、"信息"、"素材"、"脚本"、"配置"、"结果"、"参数"、"音频"、"视频"、"文件"
   ✓ 具体:"分集剧情梗概"、"角色出场时间轴"、"每分镜画面生成提示词列表"、"逐句配音时间戳对齐表"
   若现有词汇不足以精确表达,宁可自造一个新词,也不要退回泛称。

2. 字段级优先,但保持宏观:能锚定到"哪一类字段/哪一个具体产物"就锚定到那一级——
   例如不写"数据",写"每位嘉宾的姓名与头衔映射表";不写"脚本",写"分镜头拍摄脚本"。
   但只锚定"它是什么"这一宏观层面,不要展开列举它内部有哪些子字段/子结构(展开是后续递归的事)。

3. 说明是脱离上下文的术语定义:把名称当作一个新术语来定义。要求任何人只读这一行"说明",
   在完全不知道本次对话/总目标的前提下,也能准确理解:它是什么、承载什么语义、形态大致如何。
   - 写"它是什么"(信息本身),不写"它怎么来的"(来源/生成过程);
   - 不使用"上文的"、"上述"、"该项目的"、"前面提到的"等依赖上下文的指涉;
   - 是名词性的"信息/交付物",不是动词性的"动作"(写"分集大纲",不写"进行分集")。

4. 彼此正交:任意两项不得实为同一东西的不同叫法;名称不同则语义必须真正不同。`;

/* ==================== FORM:形态分类(极简,单次) ==================== */

export const FORM_SYSTEM = `判断给定交付物的形态,只回答一个标记,不做任何解释。

唯一判据:一个 LLM 逐字输出的内容,能否直接保存为这个交付物本身?

${FORM_TEXT} —— 能。纯字符文本:文章/脚本/大纲/JSON/代码/提示词/markdown/srt字幕/csv 等。
${FORM_BINARY} —— 不能,但它是数字文件。包括两类:
  · 天然二进制:视频/音频/图像/压缩包等;
  · 文本芯二进制壳:doc/docx/pdf/xlsx/pptx/epub 等——内容核心是文本,但文件本身是容器格式,LLM 无法直接输出,需要程序把文本转换/封装成该格式。
${FORM_OTHER} —— 不是数字文件:实体物品/真人行为/线下活动等。

注意:判据是"最终文件的存在形式",与内容难度无关;"要求交付 word 文档的文章"是 ${FORM_BINARY},"文章内容本身"才是 ${FORM_TEXT}。`;

export const formUser = (t: { name: string; intent: string }): string =>
    `- 名称: ${t.name} ｜ 说明: ${t.intent}\n\n它的形态是?`;

/* ==================== COMPOSE:文本成分拆解 ==================== */

// export const DIRECT_MARK = '[[direct]]';

// export const COMPOSE_SYSTEM = `你是文本结构分析员。给定一个【文本类交付物】,回答:这个文本由哪些部分构成?

// 先做一个判断:
// - 若它是单一、连贯的一体成文(一次写作即可完成,不存在性质不同的组成部分),则整份输出只写一行:${DIRECT_MARK}
// - 否则,拆出它的构成部分。

// 拆分规则:
// 1. 部分是"构成整体的更小文本",每个部分自身也是一份可独立描述的文本交付物;
// 2. 每个部分必须比整体更小、更单纯——拆出与整体一样大或一样复合的部分等于没拆;
// 3. 只拆一层:不拆部分的部分(后续会对每个部分递归处理);
// 4. 最小充分:所有部分合起来,刚好覆盖整体的全部内容,不多列;
// 5. 部分是"信息",不是"动作":写"分集大纲"(一个东西),不写"进行分集"(一个动作)。

// ${NAMING_SPEC}

// 输出格式(自然语言,非 ${DIRECT_MARK} 时):
// 一、构成部分:每项一行,严格使用:
// - 名称: <遵循命名规范:具体、唯一、可自造词> ｜ 说明: <把名称当术语定义:它是什么、承载什么语义、大致形态;脱离本对话可独立理解;不写它怎么来的>
// 二、组装说明:一句话,说明各部分齐备后如何合成整体(拼接/依模板填充/汇总改写等)。`;

// export const composeUser = (p: {
//     target: { name: string; intent: string };
//     goal: string;
//     criteria: string[];
// }): string =>
//     `【总目标(背景参考,仅供你理解,不得在命名/说明里指涉它)】${p.goal}
// 【验收标准】${p.criteria.length ? p.criteria.join('; ') : '(无)'}

// 【文本类交付物】
// - 名称: ${p.target.name} ｜ 说明: ${p.target.intent}

// 请分析其构成。`;

// export const COMPOSE_FACETS = [
//     {
//         name: '拆分判断正确性',
//         checksWhat:
//             `该拆没拆吗——整体明显由性质不同的部分复合而成(或体量大到无法一次写就),却被判了${DIRECT_MARK}?不该拆瞎拆吗——单一连贯的一体成文被强行分解?每个部分确实比整体更小更单纯吗(拆出与整体等大等复合的部分等于没拆)?`,
//     },
//     {
//         name: '组装充分性',
//         checksWhat:
//             '所有部分合起来,按"组装说明"操作,能覆盖整体"说明"承诺的全部内容吗?对照整体定义与验收标准逐项想:有没有缺件?组装说明本身成立吗(部分之间衔接得上吗)?有没有多余的部分?',
//     },
//     {
//         name: '命名具体性',
//         checksWhat:
//             '逐项检查每个部分的"名称":是否泛化(文本/内容/数据/素材/脚本/结果等一类词)?能否替换成一个唯一锚定"这一个东西"的具体名称(必要时自造词)?是否锚定到了字段级/具体产物级,而不是笼统一类?反之有没有过度展开、把内部子结构也列进了名称(应保持宏观)?',
//     },
//     {
//         name: '说明自足性',
//         checksWhat:
//             '专查每个部分的"说明":把它单独抽出、在完全不知道总目标的前提下,能明白它是什么、承载什么语义吗?是信息而非动作吗?有没有写"怎么来的"?有没有"上文的/上述/该项目的"等依赖上下文的失效指涉?各部分之间有没有两项实为同一东西?',
//     },
// ] as const;

/* ==================== CHAIN:二进制链路规划 ==================== */

export const IMPOSSIBLE_MARK = '[[impossible]]';

export const CHAIN_SYSTEM = `你是程序链路规划师。给定一个【二进制交付物】(LLM 无法直接输出),规划一条从纯文本输入到该交付物的完整程序调用链。

【你只需产出"程序调用序列",不要单独列外部输入清单】
系统会自动从你的调用序列反推:凡是某一步用到、却不由任何更早步骤产出的输入,即视为外部输入,由后续环节回到文本侧递归生成。你只需保证每一步的输入,要么引用更早步骤的产出,要么是一份 LLM 可直接写出的纯文本。

【铁律:链内二进制必须内部产出】
链中任何二进制数据(视频片段/音频/图像等),必须由链内更早的步骤产出;绝不允许把二进制素材当作外部输入(即不能出现"某步输入了一个既非纯文本、又无更早步骤产出它"的项)。

【最终输出名固定,不可改写】
目标交付物的名称由外部给定(见 user 消息中的"最终输出名")。最后一步的"产出"名称,必须与该最终输出名逐字一致,不得改动、美化或重命名。

规划规则:
1. 链是若干程序步骤的依次调用:每一步写清用什么程序、吃哪些输入、产出什么;后续步骤可以引用前面步骤的产出;最后一步的产出必须就是目标交付物;
2. 每一步都要给出该程序的具体可用候选(1~3个真实存在的工具/服务,如"可灵keling文生视频API、Runway Gen-3、通义万相"),优先取自【可用程序清单】;清单没有则提名业界成熟工具并在名称后标注(需确认);严禁发明不存在或不成熟的工具;
3. 最短路径优先:步骤越少越好。若【已有文本材料】中有能直接使用的信息,直接引用其名称以缩短链路;但不要为了迁就已有材料而绕路——你可以自由要求任何新的纯文本输入,只要整条链最短;
4. 每一步的输入,站在"真的要调用这个程序"的角度列全,漏一项程序就跑不起来;最小充分,不多列;每个输入项都要带一句"说明"(当术语定义,脱离上下文可懂),尤其是需要外部提供的纯文本输入;
5. 每个产出都是"名称+说明"二元组,严格遵循下方命名规范;链内被引用的名称必须逐字一致(不一致会断链);
6. 确无任何程序链能产出该交付物 → 整份输出只写一行:${IMPOSSIBLE_MARK} + 一句原因。

${NAMING_SPEC}

对链路而言,命名规范尤其要落到:
- 每一步的"产出"要具体到"这一步真正生成了什么这一个东西"(如"带时间戳的逐句配音音频",不是"音频");
- 二进制中间产物同样要具体命名,因为它是后续步骤的输入接口;
- 你引用的每一份纯文本输入也要具体命名(如"每分镜的图像生成提示词列表",不是"提示词")。

输出格式(自然语言):
调用序列:每步一行,严格使用:
- 第N步 ｜ 程序: <泛称> ｜ 候选: <1~3个真实工具/服务名,逗号分隔,含(需确认)标注则保留> ｜ 输入: <每项写「名称:说明」,多项用分号「;」隔开;引用更早步骤的产出名或一份纯文本输入名,须与其定义处逐字一致;说明是该输入作为术语的一句话定义> ｜ 产出: <具体名称>: <把该产出当术语定义的一句话说明>
(最后一步的产出名必须与"最终输出名"逐字一致)`;

export const chainUser = (p: {
    target: { name: string; intent: string };
    goal: string;
    criteria: string[];
    availablePrograms: string;
    availableTexts: string;
}): string =>
    `【总目标(背景参考,仅供理解,不得在命名/说明里指涉它)】${p.goal}
【验收标准】${p.criteria.length ? p.criteria.join('; ') : '(无)'}

【二进制交付物】
- 名称: ${p.target.name} ｜ 说明: ${p.target.intent}

【最终输出名(固定,最后一步产出必须逐字等于它,不得改写)】
${p.target.name}

【可用程序清单】
${p.availablePrograms}

【已有文本材料(可直接引用以缩短链路,不强求使用)】
${p.availableTexts}

请规划调用链(只产程序调用序列,不要单列外部输入)。`;

export const CHAIN_FACETS = [
    {
        name: '链路可行性',
        checksWhat:
            '每一步的程序候选真实存在且确实能完成该步的输入→产出转换吗?有没有发明不存在的工具?步骤之间衔接得上吗(每步引用的前置产出确实由更早步骤给出,且名称逐字一致)?最后一步的产出真的就是目标交付物吗(格式/形态对得上验收标准吗)?可用清单里有合适的却没用吗?',
    },
    {
        name: '程序候选具体性',
        checksWhat:
            '每一步都给出了1~3个真实、成熟的具体工具/服务候选吗?候选是真名(如"可灵API、Runway Gen-3、ffmpeg"),还是仍停留在泛称(如"视频生成引擎")?虚构或不成熟的工具要指出。清单里已有更合适候选却没选的要指出。',
    },
    {
        name: '二进制内部产出',
        checksWhat:
            '铁律核查:链中每个二进制中间物(视频/音频/图像等)都由更早的步骤产出吗?有没有把二进制素材当成了外部输入(即某步输入既非纯文本、又无更早步骤产出它)?最短路径下能否合并或删去步骤?每步输入有没有多余项?',
    },
    {
        name: '命名与接口对齐',
        checksWhat:
            '逐项检查每个"产出名"与被引用的"纯文本输入名"(末步产出名除外——它由外部固定,不得评判或改写):是否泛化(音频/脚本/参数/数据/结果等一类词)?能否替换为唯一锚定"这一个东西"的具体名称(必要时自造词)、锚定到字段级/具体产物级?是否宏观(没有把内部子结构展开进名称)?接口对齐:后续步骤引用的输入名,是否与它在更早步骤产出处或其纯文本定义处的名称逐字一致(不一致会断链)?',
    },
    {
        name: '输入完备与说明自足',
        checksWhat:
            '站在实际调用的角度逐步核对:每步输入够驱动该程序吗,漏了什么必填素材/参数?专查每个产出/纯文本输入的"说明"(末步除外):单独抽出、不知总目标时可懂吗?是信息而非动作吗?写"怎么来的"了吗?有"上文的/上述"等失效指涉吗?有两项实为同一东西吗?',
    },
] as const;

/* ============ 通用:剖面批判 / 精炼(COMPOSE 与 CHAIN 共用) ============ */

export const realizeCritiqueSystem = (facet: { name: string; checksWhat: string }): string =>
    `你是「${facet.name}」视角的专职评审。
只检查:${facet.checksWhat}
只从本视角发言,不越界--只检查语义,不检查格式类问题。用自然语言写一段意见:有问题就具体指出在哪、怎么改;没有问题只回复:${NO_ISSUE_MARK}`;

export const realizeCritiqueUser = (p: {
    targetLine: string;
    extra: string;        // CHAIN:可用程序清单+已有文本材料;COMPOSE:'(无)'
    draft: string;
    facetName: string;
}): string =>
    `【目标交付物】
${p.targetLine}

【补充材料】
${p.extra}

【待审稿】
"""${p.draft}"""

请从「${p.facetName}」视角评审。`;

export const REALIZE_REFINE_SYSTEM = `你是判定稿的修订员。给定目标交付物、补充材料、当前稿、各剖面评审意见,输出修订后的完整稿。

【修订规则】
1. 只采纳确有道理的意见;意见冲突时,以"程序真实存在、链路可执行、二进制内部产出"为最高标准;
2. 最终输出名固定:最后一步的产出名称由外部给定,不得改写/美化/重命名,任何要求改动它的意见一律不采纳;
3. 保持原稿的段落结构与行格式不变;各项仍须遵守命名与说明规范:名称具体到字段级(必要时自造词,不用泛称)、宏观不展开内部结构、彼此正交;说明把名称当术语定义、脱离上下文可懂、写"是什么"不写"怎么来的"、无失效指涉、是信息非动作;链路中引用的名称须与其定义处逐字一致;每步须给真实程序候选;
4. 纯文本输出:仅输出修订稿本身,绝对禁止输出任何推理过程、补充解释或括号注释;与原稿一致的内容原样保留,不要做任何多余的说明。`;

export const realizeRefineUser = (p: {
    targetLine: string;
    extra: string;
    draft: string;
    critiques: { facet: string; text: string }[];
}): string =>
    `【目标交付物】
${p.targetLine}

【补充材料】
${p.extra}

【当前稿】
"""${p.draft}"""

【各剖面评审】
${p.critiques.map((c) => `【${c.facet}】${c.text}`).join('\n\n')}

请输出修订后的完整稿。`;

/* ==================== EXTRACT:唯一的 JSON 提取 ==================== */

const TermSchema = z.object({
    name: z
        .string()
        .describe('该项「名称:」后的内容,原样搬运。应是一个具体、唯一、脱离上下文可辨识的交付物/信息名(可能是自造词)'),
    intent: z
        .string()
        .describe('该项「说明:」后的内容,原样搬运。它是该名称作为术语的完整定义,脱离本对话也能理解其语义'),
});

export const ComposeSchema = z.object({
    parts: z.array(TermSchema).describe('「构成部分」逐项:名称→name,说明→intent'),
    assembly: z.string().describe('「组装说明」一句话,原样提取'),
});
export type ComposeOut = z.infer<typeof ComposeSchema>;

export const ChainStepSchema = z.object({
    program: z.string().describe('该步「程序:」后的泛称'),
    candidates: z
        .array(z.string())
        .describe('该步「候选:」逗号分隔的真实工具/服务名,原样逐项搬运;含(需确认)标注一并保留'),
    inputs: z
        .array(
            z.object({
                name: z.string().describe('该输入项的名称,须与其在更早步骤产出处或其纯文本定义处的名称逐字一致'),
                intent: z.string().describe('该输入项作为术语的一句话定义:它是什么、承载什么语义;脱离本对话可懂;写"是什么"不写"怎么来的"'),
            }),
        )
        .describe('该步「输入:」逐项,每项为 名称+说明'),
    output_name: z.string().describe('该步「产出:」的名称部分,应为具体、唯一的产物名(可能是自造词);末步须与最终输出名逐字一致'),
    output_intent: z.string().describe('该步「产出:」冒号后的一句话说明,是该产出作为术语的定义,脱离上下文可懂'),
});

export const ChainSchema = z.object({
    steps: z.array(ChainStepSchema).describe('「调用序列」逐步,保持原有顺序'),
});
export type ChainStep = z.infer<typeof ChainStepSchema>;
export type ChainOut = z.infer<typeof ChainSchema>;